# 常见问题

## Q：绑定了箱子、AE 系统或其他储存空间，但不能把东西存进去怎么办？

先检查绑定时是不是对目标右键了。

右键绑定是“仅提取”模式，无法存入物品。

如果你想让这个储存同时支持存入和取出：

1. 打开 RTSBuilding。
2. 点击顶部栏的“储存绑定”按钮。
3. 对准当前绑定的箱子、AE 接口或储存控制器，左键点击一次，取消这次绑定。
4. 再对准同一个目标左键点击一次，重新绑定。
5. 确认顶部提示显示已绑定储存。

重新左键绑定后，这个储存就可以同时存入和取出了。
